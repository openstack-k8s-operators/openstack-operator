apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: transporturls.rabbitmq.openstack.org
spec:
  group: rabbitmq.openstack.org
  names:
    kind: TransportURL
    listKind: TransportURLList
    plural: transporturls
    singular: transporturl
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Status
      jsonPath: .status.conditions[0].status
      name: Status
      type: string
    - description: Message
      jsonPath: .status.conditions[0].message
      name: Message
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: TransportURL is the Schema for the transporturls API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TransportURLSpec defines the desired state of TransportURL
            properties:
              rabbitmqClusterName:
                description: RabbitmqClusterName the name of the Rabbitmq cluster
                  which to configure the transport URL
                type: string
              userRef:
                description: UserRef - reference to a RabbitMQUser resource. If not
                  specified, Username will be used to create a RabbitMQUser on-demand
                type: string
              username:
                description: Username - RabbitMQ username. If UserRef is not specified,
                  a RabbitMQUser will be created with this username
                type: string
              vhost:
                description: Vhost - RabbitMQ vhost name. If specified and vhost doesn't
                  exist, a RabbitMQVhost will be created. Defaults to "/" if not specified
                type: string
            required:
            - rabbitmqClusterName
            type: object
          status:
            description: TransportURLStatus defines the observed state of TransportURL
            properties:
              conditions:
                description: Conditions
                items:
                  description: Condition defines an observation of a API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition
                        in CamelCase.
                      type: string
                    severity:
                      description: |-
                        Severity provides a classification of Reason code, so the current situation is immediately
                        understandable and could act accordingly.
                        It is meant for situations where Status=False and it should be indicated if it is just
                        informational, warning (next reconciliation might fix it) or an error (e.g. DB create issue
                        and no actions to automatically resolve the issue can/should be done).
                        For conditions where Status=Unknown or Status=True the Severity should be SeverityNone.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of condition in CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: |-
                  ObservedGeneration - the most recent generation observed for this
                  service. If the observed generation is less than the spec generation,
                  then the controller has not processed the latest changes injected by
                  the opentack-operator in the top-level CR (e.g. the ContainerImage)
                format: int64
                type: integer
              queueType:
                description: QueueType - the queue type from the associated RabbitMq
                  instance
                type: string
              rabbitmqUserRef:
                description: |-
                  RabbitmqUserRef - the name of the RabbitMQUser CR that was created or referenced by this TransportURL.
                  Empty if using default cluster admin credentials (no dedicated RabbitMQUser CR)
                type: string
              rabbitmqUsername:
                description: RabbitmqUsername - the actual username used for the RabbitMQ
                  user
                type: string
              rabbitmqVhost:
                description: RabbitmqVhost - the actual vhost name used
                type: string
              secretName:
                description: SecretName - name of the secret containing the rabbitmq
                  transport URL
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
