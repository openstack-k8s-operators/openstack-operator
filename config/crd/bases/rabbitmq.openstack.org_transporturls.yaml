---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: transporturls.rabbitmq.openstack.org
spec:
  group: rabbitmq.openstack.org
  names:
    kind: TransportURL
    listKind: TransportURLList
    plural: transporturls
    singular: transporturl
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: TransportURL is the Schema for the transporturls API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TransportURLSpec defines the desired state of TransportURL
            properties:
              rabbitmqClusterName:
                description: RabbitmqClusterName the name of the Rabbitmq cluster
                  which to configure the transport URL
                type: string
            type: object
          status:
            description: TransportURLStatus defines the observed state of TransportURL
            properties:
              secretName:
                description: SecretName - name of the secret containing the rabbitmq
                  transport URL
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
