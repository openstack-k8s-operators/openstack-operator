---
- project:
    name: openstack-k8s-operators/openstack-operator
    github-check:
      jobs:
        - openstack-operator-content-provider
        - openstack-operator-crc-extended-podified-deployment: &content_provider
            dependencies:
              - openstack-operator-content-provider
