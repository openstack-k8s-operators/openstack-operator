apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |
      oc apply -n $NAMESPACE -f ../../../../config/samples/core_v1beta1_openstackcontrolplane_galera_3replicas.yaml
  - script: |
      # Disable the services that are not needed for the test (these are also already covered in the ctlplane-basic-deployment test)
      NAMESPACE=$NAMESPACE ../../common/toggle_services.sh openstack-galera-3replicas "barbican,cinder,swift,telemetry" false
